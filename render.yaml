services:
  - type: web
    name: sakro-nestjs-api
    runtime: node
    buildCommand: npm ci && npm run build:render
    startCommand: npm run start:prod
    envVars:
      - key: NODE_ENV
        value: production
